## Authentasaurus configuration
development: &non_production_settings
  :mail:
    :email:     "foo_bar@your-domain.com"
  :modules:
    :recoverable:
      :token_expires_after: 10 # days

test:
  <<: *non_production_settings

production:
  :mail:
    :email:     "foo_bar@your-domain.com"
  :modules:
    :recoverable:
      :token_expires_after: 10 # days
